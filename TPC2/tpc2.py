import numpy as np

'''
Métodos de feature selection podem seguir a estrutura de
um Transformer.
• Arquitetura de um Transformer (semelhante ao scikit-learn)
– Argumentos – passados pelo utilizador
– Parâmetros estimados – conjunto de parâmetros/atributos estimados a
partir dos dados
– fit – método responsável por estimar parâmetros a partir dos dados
– transform – método responsável por transformar os dados
'''

# VarianceThreshold

# f_classif: ANOVA (one-way ANOVA– equivalente a t-test para 2 classes)

# SelectKBest 